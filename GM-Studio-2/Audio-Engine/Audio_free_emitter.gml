/// @description Free up emitter

//////////////////////////////////////////////////////////////////////////
/// This should be used on the events: game_end and destroy
//////////////////////////////////////////////////////////////////////////

if(audio_emitter_exists(emitter_main)) {
	audio_emitter_free(emitter_main);
}
